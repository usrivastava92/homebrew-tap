# Homebrew Tap

Homebrew tap for apps published by `usrivastava92`.

## Install

```bash
brew install --cask usrivastava92/tap/gtv-desktop-remote
```

Or tap once:

```bash
brew tap usrivastava92/tap
brew install --cask gtv-desktop-remote
```
